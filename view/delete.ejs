<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Delete User</title>
    <style>
        form {
            display: flex;
            background: rgba(177, 252, 255, 0.774);
            flex-direction: column;
            margin: 10px;
            width: 50%;
        }
        input {
            min-width: 250px;
            width: fit-content;
            margin: 10px 0px 10px 10px;
        }
        button {
            min-width: 100px;
            margin: 10px 0px 10px 10px;
            width: fit-content;
        }
    </style>
</head>
<body>
    <form action="/user/<%= user.id %>?_method=DELETE" method="post">
        <h1> You are about to delete this user: <%= user.username %></h1>
        <input type="email" name="email" placeholder="Enter email ID">
        <input type="password" name="password" placeholder="Enter password">
        <button>Submit and Delete</button>
    </form>
</body>
</html>